# Copia este archivo como .env. Nunca expongas esta clave en VITE_* ni en React.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TRANSCRIBE_MODEL=whisper-1

ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=EXAVITQu4vr4xnSDxMaL
ELEVENLABS_MODEL=eleven_multilingual_v2

FRONTEND_ORIGIN=http://localhost:5173
DATABASE_PATH=./clara.db
GENERATED_DIR=./generated
VOICE_CACHE_DIR=./voice_cache

# Opcionales: sin ellos el envío se simula y el demo no se interrumpe.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
RESEND_API_KEY=
# Resend rechaza (403) cualquier dominio propio sin verificar en
# resend.com/domains. Para el demo, usa el remitente de pruebas de Resend
# (no requiere verificación) en vez de un dominio inventado:
REPORT_FROM_EMAIL="CLARA <onboarding@resend.dev>"
