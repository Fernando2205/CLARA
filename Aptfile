# Opcional: solo se usa si agregas el buildpack heroku-community/apt.
# Sin este buildpack, WeasyPrint falla al importar (faltan estas librerías
# de sistema) y el backend genera el PDF con reportlab en su lugar — el
# endpoint /report sigue funcionando igual, con una plantilla más simple.
# Agrega el buildpack solo si quieres la plantilla PDF con el diseño completo.
libpango-1.0-0
libpangocairo-1.0-0
libcairo2
libgdk-pixbuf2.0-0
libffi-dev
shared-mime-info
fonts-liberation
